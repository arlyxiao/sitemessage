.page-new-message
  -if !@receiver.blank?
    .send-to 发送短消息给 #{@receiver.name}
      
    = flash.now[:error]
    
    = form_for @short_message do |f|
      #short-content
        %input{:name=>'short_message[receiver_id]', :type=>'hidden', :value=>@receiver.id}
        %textarea{:name=>'short_message[content]'}
        .field
          %a.form-submit-button{:href=>'javascript:;'} 发送
  -else
    .receiver-blank 没有指定收信者
